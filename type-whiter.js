//O efeito maquina de escrever também pode ser utilizado através do JS.
//Mas nessa aplicação o CSS teve mais utilidade !
//Abaixo está a atulizaçaõ através do JS:  


/*
function typeWriter(elemento) {
    const textoArray = elemento.innerHTML.split('');
    elemento.innerHTML = '';
    textoArray.forEach((letra, i) => {
        setTimeout(() => elemento.innerHTML += letra, 75 * i)
    });
}

const paragrafos = document.querySelector('p');

typeWriter(document.querySelector('.paragrafo'));
 */


